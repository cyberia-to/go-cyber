# graph-filter

This is a simple library to filter a graph based on a set of rules.
Main usage for cyb.ai is to filter a graph of a knowledge graph based on a set of filtered particles which registry maintain group of admins.
